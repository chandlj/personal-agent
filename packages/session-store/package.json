{
  "name": "@personal-agent/session-store",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "check": "bunx tsc -p tsconfig.json --pretty",
    "build": "bun run check",
    "db:generate": "bunx drizzle-kit generate --config drizzle.config.ts"
  },
  "dependencies": {
    "@personal-agent/config": "workspace:*",
    "@personal-agent/shared": "workspace:*",
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.10"
  },
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  }
}
